# Development overrides (`inngest` dev mode, `o2`, dev credentials).

services:
  postgres:
    environment:
      # Development-only credentials!
      POSTGRES_PASSWORD: password

  inngest:
    command:
      - inngest
      - dev
      - '--no-discovery'
      - '--sdk-url'
      - 'http://host.docker.internal:5173/api/inngest' # Vite

  o2:
    environment:
      # Development-only credentials!
      ZO_ROOT_USER_EMAIL: admin@example.com
      ZO_ROOT_USER_PASSWORD: Password123*

  rustfs:
    environment:
      RUSTFS_OBS_ENDPOINT: http://o2:5080/api/default
      RUSTFS_OBS_ENDPOINT_HEADERS: Authorization=Basic%20YWRtaW5AZXhhbXBsZS5jb206UGFzc3dvcmQxMjMq
      # Development-only credentials!
      RUSTFS_ACCESS_KEY: drap
      RUSTFS_SECRET_KEY: password

  setup-bucket:
    environment:
      MC_HOST_drap: http://drap:password@rustfs:9000
